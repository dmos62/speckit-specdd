@# Surface non-mutating toolchain, bootstrap, and worktree state to the next iteration.
@cli! bash -c "node --version && npm --version && uv --version"
@cli! bash -c "bash scripts/bootstrap.sh --check"
@cli! bash -c "git status --short"
